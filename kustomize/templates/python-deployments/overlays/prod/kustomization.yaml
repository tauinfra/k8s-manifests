apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

images:
  - name: registry.commany.com/python-deployment
    newTag: prod

replicas:
  - name: python-deployment
    count: 2

patchesStrategicMerge:
  - patch.yaml

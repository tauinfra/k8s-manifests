apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      app.kubernetes.io/name: go-deployment
      app.kubernetes.io/component: backend
      app.kubernetes.io/part-of: devops

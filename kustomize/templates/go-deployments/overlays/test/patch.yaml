# overlays/dev/patch.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-deployment
spec:
  template:
    spec:
      containers:
        - name: go-deployment
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "200m"
              memory: "512Mi"
apiVersion: v1
kind: Service
metadata:
  name: springboot-deployment
spec:
  selector:
    app.kubernetes.io/name: springboot-deployment
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP